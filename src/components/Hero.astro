<!-- Hero Section -->
<section
    class="relative overflow-hidden flex items-center container mx-auto px-4 max-w-5xl pt-32 pb-12"
    id="hero"
>
    <!-- Arrière-plan flou spécifique au hero -->
    <div class="absolute inset-0 -z-10">
        <div
            class="absolute inset-0 bg-cover bg-center bg-no-repeat"
            style="background-image: url('/pizza-hero-background.jpg'); filter: blur(5px); transform: scale(1.05);"
        >
        </div>
        <div class="absolute inset-0 bg-black/50"></div>
    </div>

    <div class="container mx-auto px-4 relative">
        <div class="text-center flex flex-col justify-center space-y-4">
            <p
                class="text-sm text-white/90 max-w-2xl mx-auto leading-relaxed font-medium drop-shadow-lg"
            >
                La pizzeria artisanale à <strong class="text-white"
                    >Capbreton</strong
                >. <br /> Découvrez nos pizzas savoureuses préparées avec soin par
                nos chefs passionnés.
            </p>

            <h2
                class="text-2xl sm:text-3xl md:text-4xl font-bold text-white font-panpizza drop-shadow-lg"
            >
                Pizzas à emporter à Capbreton
            </h2>
        </div>
    </div>
</section>
